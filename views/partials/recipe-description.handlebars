<div class="recipe w-50">
    <h2>{{ name }}</h2>
    <div class="simple-border">
        <p>{{ description }}</p>
    </div>
    {{#each icons }}
        <img class="allergen-icon" src="/icons/{{ this }}.png" alt="{{ this }}">
    {{/each}}
</div>