<div class="recipe card m-3">
    <div class="card-body d-flex flex-column">

        <div class="d-flex">
        {{#each ingredientsClean }}
        <div>{{this.ingredient}}</div>
        {{/each}}
        </div>
    </div>
</div>