{{> allergens }}
<div class="container mt-5">

    <h1 class="text-center">Your Recipes</h1>
<div class="text-center">
    <a href="/userprofile/add-recipe" id="add-recipe" class=" btn btn-secondary">Add a New Recipe</a>
   </div>
    <div id="top-recipes" class="d-flex flex-wrap justify-content-center mt-4">
        {{#each recipes as |recipe|}}
            {{> recipe-description}}
        {{/each}}
    </div>
</div>
