<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>The Allergy Free Kitchen</title>
</head>

<body>
    <header>
        <nav class="navbar navbar-light">
            <div class="container-fluid d-flex">
                <a href="/" class="navbar-brand">The Allergy Free Kitchen</a>
                {{#if loggedIn}}
                <a href="/userprofile" class="navbar-brand">Welcome To <span class="fw-bold">{{ name }}</span>'s Kitchen</a>
                <a href="/add-recipe" id="add-recipe" class="btn btn-secondary">Create a Recipe</a>
                <button id="logout" class="btn block">logout</button>
                {{else}}
                <button id="login" class="btn block">login</button>
   
                {{/if}}
            </div>
        </nav>
    </header>
    {{{body}}}
    
<script src="/javascript/allergen-filter.js"></script>
    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{else}}
    <script>
        async function login() {
            document.location.replace('/login');
        }

        document.querySelector('#login').addEventListener('click', login);
    </script>
    {{/if}}
</body>

</html>